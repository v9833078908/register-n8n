# 🚀 Инструкция по запуску YouTube→Threads MVP

## 📋 Что нужно подготовить

### 1. API ключи и токены

#### YouTube Channel ID
1. Откройте свой YouTube канал
2. URL будет вида: `https://www.youtube.com/@username`
3. Нужен Channel ID (можно получить через [этот сервис](https://commentpicker.com/youtube-channel-id.php))

#### Anthropic API Key (Claude)
1. Зарегистрируйтесь на https://console.anthropic.com/
2. Создайте API ключ в разделе API Keys
3. Стоимость: ~$0.003 за пост (модель Claude 3.5 Sonnet)

#### Threads API Access Token
1. Нужен Instagram Business/Creator аккаунт, подключённый к Threads
2. Создайте Facebook App на https://developers.facebook.com/
3. Настройте Threads API permissions
4. Получите долгосрочный access token
5. Получите User ID через API: `https://graph.threads.net/v1.0/me?fields=id&access_token=YOUR_TOKEN`

Документация Threads API: https://developers.facebook.com/docs/threads

#### Telegram Bot Token
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Получите токен вида `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
4. Отправьте `/start` вашему боту
5. Получите свой Chat ID через https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates

---

## ⚙️ Установка

### 1. Клонируйте репозиторий
```bash
git clone <repository_url>
cd youtube-threads-mvp
```

### 2. Создайте виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

Откройте `.env` и заполните ваши данные:
```env
# YouTube
YOUTUBE_CHANNEL_ID=your_channel_id_here
CHECK_INTERVAL_HOURS=6

# Anthropic API (Claude)
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# Threads API
THREADS_ACCESS_TOKEN=your_threads_token_here
THREADS_USER_ID=your_threads_user_id_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=your_chat_id_here

# Database
DATABASE_URL=sqlite:///./youtube_threads_mvp.db

# Настройки модерации
MIN_TRANSCRIPT_LENGTH=100
MIN_WORD_COUNT=20
MAX_POST_LENGTH=500
```

---

## 🎯 Первый запуск

### 1. Проверьте конфигурацию

Запустите тесты, чтобы убедиться что всё работает:
```bash
python -m pytest tests/test_database.py -v
python -m pytest tests/test_youtube_detector.py -v
python -m pytest tests/test_transcriber_v2.py -v
```

### 2. Инициализируйте базу данных
```bash
python -c "from src.database.db import Database; db = Database(); db.create_tables(); print('Database initialized!')"
```

### 3. Запустите MVP

**Опция A: Полный автоматический режим (будет проверять YouTube каждые N часов)**
```bash
python main.py --auto
```

**Опция B: Проверка один раз**
```bash
python main.py --once
```

**Опция C: Обработать конкретное видео**
```bash
python main.py --video-url "https://www.youtube.com/watch?v=VIDEO_ID"
```

---

## 📱 Работа через Telegram

После запуска системы:

1. **Новое видео найдено** → Бот отправит транскрипцию
2. **Генерация постов** → Бот покажет 3-5 вариантов постов
3. **Модерация** → Вы увидите результаты проверки качества
4. **Утверждение** → Нажмите кнопку "✅ Опубликовать" или "❌ Отклонить"
5. **Публикация** → Пост публикуется в Threads, бот отправляет ссылку

### Команды бота:
- `/start` - Начать работу
- `/status` - Статус системы
- `/pending` - Показать ожидающие утверждения посты
- `/stats` - Статистика

---

## 🔧 Настройка промптов

Отредактируйте `config/prompts/threads.yaml` для изменения стиля генерации постов:

```yaml
system_prompt: |
  Ты эксперт по созданию engaging контента для платформы Threads.

  Твоя задача - создать 3-5 коротких постов на основе транскрипции YouTube Shorts.

  Структура поста:
  - Начни с релевантного эмодзи (🎯, 💡, 🔥, 🎬)
  - Hook - интересный факт или вопрос
  - Основная мысль (2-3 предложения)
  - Key takeaway с эмодзи 💡
  - Call-to-action или вопрос для engagement
  - 1-2 хештега в конце

  Стиль:
  - Естественный разговорный язык
  - Простые предложения
  - Эмоциональный и личный тон
  - Избегай официоза

  Длина: 300-450 символов (не более 500)

user_template: |
  Создай 3-5 вариантов постов для Threads на основе этой транскрипции видео:

  Название: {title}

  Транскрипция:
  {transcript}

  Требования:
  - Каждый пост должен быть уникальным
  - Используй разные hooks и углы подачи
  - Добавь 1-2 релевантных хештега в конец
  - Максимум 500 символов
  - Нумеруй посты: "Вариант 1:", "Вариант 2:" и т.д.
```

---

## 🛡️ Настройка модерации (guardrails)

Отредактируйте `config/guardrails.yaml`:

```yaml
# Проверка транскрипции
transcript_validation:
  min_length: 100           # Минимум символов
  min_word_count: 20        # Минимум слов
  max_length: 50000         # Максимум символов
  max_repetition_ratio: 0.5 # Макс. процент повторений (0.5 = 50%)
  min_alpha_ratio: 0.5      # Мин. процент букв (остальное - цифры/символы)

# Проверка постов
post_validation:
  platform_limits:
    threads:
      max_length: 500       # Лимит Threads
      min_length: 20
      max_hashtags: 5
      max_emojis: 10

  quality_checks:
    spam_patterns:
      - "!{3,}"              # Много !!!
      - "[А-ЯA-Z]{20,}"      # Длинный КАПС
      - "(.)\\1{10,}"        # Повтор символов
```

---

## 🐛 Решение проблем

### Ошибка: "youtube-transcript-api: TranscriptsDisabled"
**Решение:** Видео не имеет субтитров. Включите автосубтитры на YouTube или дождитесь другого видео.

### Ошибка: "Anthropic API Error: 401 Unauthorized"
**Решение:** Проверьте правильность API ключа в `.env`

### Ошибка: "Threads API Error: Invalid OAuth token"
**Решение:**
1. Токен мог истечь, получите новый
2. Проверьте что User ID соответствует токену

### Бот Telegram не отвечает
**Решение:**
1. Проверьте токен бота
2. Убедитесь что вы отправили `/start` боту
3. Проверьте Chat ID командой `getUpdates`

### База данных заблокирована (SQLite locked)
**Решение:** Закройте все процессы, использующие БД:
```bash
pkill -f main.py
rm youtube_threads_mvp.db-journal  # Если существует
```

---

## 📊 Мониторинг

### Проверить статус базы данных
```bash
python -c "
from src.database.db import Database
db = Database()
print(f'Total videos: {len(db.get_all_videos())}')
print(f'Pending approval: {len(db.get_pending_videos())}')
"
```

### Посмотреть логи
```bash
tail -f youtube_threads.log
```

---

## 🚀 Деплой на сервер

### Вариант 1: Systemd service (Linux)

Создайте `/etc/systemd/system/youtube-threads.service`:
```ini
[Unit]
Description=YouTube to Threads Automation
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/youtube-threads-mvp
Environment="PATH=/path/to/youtube-threads-mvp/venv/bin"
ExecStart=/path/to/youtube-threads-mvp/venv/bin/python main.py --auto
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable youtube-threads
sudo systemctl start youtube-threads
sudo systemctl status youtube-threads
```

### Вариант 2: Docker

Создайте `Dockerfile`:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py", "--auto"]
```

Запустите:
```bash
docker build -t youtube-threads-mvp .
docker run -d --name youtube-threads \
  --env-file .env \
  -v $(pwd)/youtube_threads_mvp.db:/app/youtube_threads_mvp.db \
  youtube-threads-mvp
```

### Вариант 3: Cron (проверка раз в N часов)

Добавьте в crontab (`crontab -e`):
```bash
# Проверять каждые 6 часов
0 */6 * * * cd /path/to/youtube-threads-mvp && /path/to/venv/bin/python main.py --once >> logs.txt 2>&1
```

---

## 📈 Рекомендации по использованию

### Частота проверки YouTube
- **Для активных каналов (>1 Shorts/день):** `CHECK_INTERVAL_HOURS=3`
- **Для умеренных (2-3 Shorts/неделя):** `CHECK_INTERVAL_HOURS=6` ✅ рекомендуется
- **Для редких (1-2 Shorts/месяц):** `CHECK_INTERVAL_HOURS=12`

### Стиль постов
Изучите топовых блогеров в вашей нише в Threads и адаптируйте промпты в `config/prompts/threads.yaml`

### Модерация
- Первые 10-20 постов проверяйте вручную
- Если качество стабильное, можно добавить автопубликацию (будет в следующей версии)

---

## 🔐 Безопасность

### ⚠️ ВАЖНО:
1. **Никогда не коммитьте `.env`** в git!
2. Используйте долгосрочные токены Threads (60 дней) и настройте автообновление
3. Ограничьте Telegram бота только вашим Chat ID
4. Регулярно обновляйте зависимости: `pip install --upgrade -r requirements.txt`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `tail -f youtube_threads.log`
2. Запустите тесты: `pytest -v`
3. Создайте issue в репозитории с подробным описанием

---

## 🎉 Готово!

Теперь ваша система автоматически:
1. ✅ Мониторит новые Shorts на YouTube
2. ✅ Извлекает транскрипцию
3. ✅ Генерирует качественные посты через Claude
4. ✅ Проверяет на спам и качество
5. ✅ Отправляет вам на утверждение в Telegram
6. ✅ Публикует в Threads после вашего подтверждения

Приятного использования! 🚀
